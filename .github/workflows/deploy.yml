name: Deploy to GitHub Pages

on:
  workflow_dispatch:  # retired 2026-09-19 — Cloudflare Pages (cloudflare-pages.yml) is the live deploy

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        working-directory: astro-portfolio
        run: bun install --frozen-lockfile

      - name: Generate OG images
        working-directory: astro-portfolio
        run: |
          echo "📸 Generating OG images..."
          bun add -D sharp
          bun run generate:og-image

      - name: Build static site
        working-directory: astro-portfolio
        run: bun run build

      - name: Copy CNAME for custom domain
        run: cp CNAME astro-portfolio/dist/CNAME

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: astro-portfolio/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
